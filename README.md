因为我老婆平时喜欢拼豆，所以做了这个项目，从开始做，到第一版发布断断续续持续了大概三个月，现在大部分功能都没问题了，简单图纸识别几乎不会出错，复杂的大型图纸还是需要人手工校正，希望大家会喜欢，有使用上的问题和意见可以发送到我的邮箱：pseriaml@163.com

# 🧩 AI拼豆助手

AI拼豆助手是一款**AI拼豆图纸识别工具**，它会自动识别每个格子里的编码（如 H7、F2、C21），对照品牌颜色库，生成一份清晰、可读的电子图纸，当前仅支持 mard 品牌的豆子颜色编码。

---

## 一、安装与使用（普通用户）

### 1. 安装（三步搞定，无需安装任何软件）

#### 第 1 步：下载并解压应用

- 拿到应用压缩包 `bead-local-server-v0.1.1.zip`（随发布提供）
- 解压到任意目录（如 `D:\AI拼豆助手\`），双击进去能看到 `start-local.bat`

#### 第 2 步：下载并安装识别模型（首次使用必做）

识别模型体积较大，没有打包进应用，需要从网盘下载一次：

1. **打开网盘链接**（夸克网盘）：`https://pan.quark.cn/s/ef8e5bac36ad`
2. **下载模型压缩包**（`.zip`，文件名类似 `bead-models-...zip`）
3. **解压到应用目录的 `models` 文件夹**：

   ```
   <你的解压目录>\models\                （例如 D:\AI拼豆助手\models\）
   ```

   解压完成后确认结构（**只有一层模型目录**）：

   ```
   models\
   ├── bean-mard-v12-2026-08-15T10-39-29Z\   ← 模型目录（带版本和日期）
   │   ├── model.onnx          ← 识别引擎用的模型文件
   │   ├── charset.json        ← 识别字符集
   │   ├── manifest.json       ← 模型信息
   │   └── ...（其余文件保留即可，不用管）
   └── model-current.txt       ← 当前使用的模型（一般不用动）
   ```

   ⚠️ **常见错误**：解压后变成 `models\models\bean-mard-...\`（多套一层目录）。解压时选「解压到当前目录」，并检查 `model.onnx` 能直接在模型目录里看到。

#### 第 3 步：启动

- **启动**：双击 `start-local.bat` —— 后台启动服务，并自动打开浏览器页面
  - 打开的地址是**本机的局域网地址**（形如 `http://192.168.x.x:5173`），**同一局域网内的其他设备（手机/平板/其他电脑）也可以直接访问这个地址**使用 AI拼豆助手
  - 本机自己也可以随时用 `http://localhost:5173` 访问
  - ⚠️ **注意**：自动打开的局域网地址是程序检测到的，在个别网络环境（多网卡、虚拟机、代理软件等）下**可能不是真实可达的地址**。如果浏览器打不开或局域网设备访问不了：
    1. 在**本机**打开命令行输入 `ipconfig`，找到当前联网网卡的 IPv4 地址（形如 `192.168.x.x` 或 `10.x.x.x`）
    2. 在浏览器里手动访问 `http://<这个IP>:5173`
- **停止**：双击 `stop-local.bat`
- **重启**：先停止、再启动

### 2. 使用说明

1. **上传**：打开页面，点「上传图纸」，选择图片（图纸照片或截图均可，支持 JPG/PNG，30MB 以内）
2. **框选网格**：拖拽调整裁剪框，框住图纸的网格区域；填入**行数、列数**（图纸上一般会标，如 80 行 × 158 列）
3. **开始识别**：点击开始，任务约几十秒到 1 分钟（格子越多越久），可实时看到进度
4. **查看图纸**：识别完成后进入图纸详情——每个格子显示**编码 + 颜色**，右侧是颜色图例
5. **手动校正**：识别不准的格子，去「校正」页手动修改（支持 Shift+点击 矩形批量选择）；蓝色标记可恢复原识别
6. **导出**：需要照着拼的时候，可导出包含逐格图纸和色号统计的资料包
7. **手机/平板使用**：同一 WiFi 下，其他设备浏览器访问启动时自动打开的地址 `http://<本机IP>:5173` 即可（启动窗口会显示本机局域网 IP；该地址与自动打开的浏览器地址相同）

> 💌 **帮助改进**：如果你在使用中发现识别错误，请在「校正」页修改后**导出校正数据**，发送到我的邮箱 **pseriaml@163.com**。每一个校正样本都能帮助我改进识别模型，让 AI拼豆助手越来越聪明，先谢过大家！

### 3. 常见问题

| 问题 | 解决办法 |
|------|----------|
| 双击后浏览器打不开页面 | 看 `data\server.err.log` 的报错；最常见原因是**模型没装好**（回到安装第 2 步检查目录结构） |
| 识别结果大量是 UNMAPPED（灰色斜线格） | 图纸照片不够清晰/反光/拍歪，换更清晰的图重试；少量格子可在校正页手动改 |
| 想换一个模型版本 | `models` 目录可以同时放多个版本；改 `models\model-current.txt` 里的目录名，重启即可 |
| 手机访问不了 | 确认手机与电脑在同一局域网（同一 WiFi）；如仍不行，可能是**系统防火墙拦截**了 5173 端口——需要你自行决定是否放行（以**管理员身份**打开命令行运行：`netsh advfirewall firewall add rule name="bead-local-server" dir=in action=allow protocol=TCP localport=5173`；不再需要时可运行 delete rule 删除） |
| 数据存在哪里 | 全部在本机：数据库 `data\bead-local.db`、上传图片 `uploads\`，不联网、不传云端 |

---

## 二、原理与项目结构（简介）

### 工作原理

```
图纸图片 → 框选网格区域 → 逐格 OCR 识别编码 → 对照颜色库 → 生成可读电子图纸
```

1. 用户上传图纸图片并框选出网格区域（行列数由用户指定）
2. 程序把网格切成一个个格子，用 **CRNN 文字识别模型**（OCR）识别格子内印刷的字母数字编码（如 `H7`、`F2`、`C21`）
3. 每个编码对照**拼豆颜色库**（mard 291 色）找到对应颜色；库里没有的编码标记为 UNMAPPED
4. 生成**只读电子图纸**：逐格颜色 + 编码 + 色号，支持手动校正与导出

### 项目结构

| 目录 | 作用 |
|------|------|
| `local_server/` | **主程序**（Rust 单文件）：网页服务 + 识别引擎 + 数据存储，一个 exe 全搞定 |
| `frontend/` | 网页界面（React）：上传、图纸查看、校正、颜色库 |
| `training/` + `ocr_core/` | 模型训练工具（开发用，运行时不需要） |

### 技术栈

Rust（axum）+ SQLite + ONNX Runtime（识别引擎）；React + TypeScript（界面）；PyTorch（模型训练）。

### 开发者

开发、测试、模型训练等完整命令与架构细节见 **`AGENTS.md`**；架构决策记录见 `docs/adr/`。

---

## 开源协议

本项目采用 **GNU Affero 通用公共许可证 v3（AGPL-3.0）** 开源：

- ✅ **允许**：自由使用、复制、修改、再分发，包括商业用途
- ✅ **必须**：保留原作者版权声明与出处（不得移除 LICENSE、作者信息及来源声明）
- 📖 **开源义务**：无论分发软件还是通过**网络提供服务**，都必须以 AGPL-3.0 向使用者开放源码
- 📧 其他授权或合作需求，请联系作者：**pseriaml@163.com**

协议全文：<https://www.gnu.org/licenses/agpl-3.0.html>
