# Python
__pycache__/
*.pyc
*.pyo
.env
*.egg-info/
.pytest_cache/

# Frontend
node_modules/
dist/
frontend/dist/
frontend/.vite/
frontend/coverage/
# 测试用大图（2.2MB，不应入库）
frontend/public/test.jpg

# Training
training/__pycache__/
training/**/__pycache__/
# Only IMAGE files inside training data directories are NOT tracked.
# Non-image artifacts (label tool, manifest JSON, .gitkeep) are tracked.
# Each developer trains their own model locally — see training/README.md.
training/samples/
# Trained model checkpoints
training/checkpoints/*.pt
training/data/

# Workspace / tooling
.conda/
.idea/
.playwright-mcp/
.claude/
.gstack/
.superpowers/
.pi-subagents/
.scratch/

# Runtime uploads
uploads/
examples/

# Generic dev / OS / tooling artifacts
*.log
*.bak
*.tmp
*.swp
*~
*.orig
*.rej
.DS_Store
Thumbs.db
.env.local
.env.development
.env.production
.env.*.local
frontend/.env
frontend/.env.*
*.db
*.sqlite3
*.sqlite

# Python virtualenvs
.venv/
venv/
env/
ENV/

# IDE / editor dirs
.vscode/
.vs/

# Tool caches
.mypy_cache/
.ruff_cache/
.ipynb_checkpoints/
htmlcov/
.coverage
coverage.xml
*.cover

# Build output
build/

# ── 新架构（2026-08 Rust 重写）──

# 模型 artifact：权重不入库（library.json 快照提交；model.pt/onnx 由用户按 README 从网盘分发）
artifacts/models/**/model.pt
artifacts/models/**/model.onnx
artifacts/models/current

# local_server 构建产物与发布包
local_server/target/
local_server/release/
local_server/bead-local-server-v*.zip

# Legend OCR engine (PP-OCRv5, distributed via 网盘 like the CRNN model)
local_server/models/*.onnx
local_server/models/*.txt

# Runtime data (SQLite DB, uploads) — never commit
local_server/data/
data/
